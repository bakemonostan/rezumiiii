const useToast = ref(false);
const useToastClose = ref(false);
const useToastEdit = ref(false);
const useToastDelete = ref(false);
const useToastEmailConfirmed = ref(false);
export {
  useToast,
  useToastClose,
  useToastDelete,
  useToastEdit,
  useToastEmailConfirmed,
};
