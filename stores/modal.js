const useModal = ref(false);
const useCreateModal = ref(false);
const useEditModal = ref(false);
const useShareModal = ref(false);
const useCloseModal = ref(false);
const useDeleteModal = ref(false);
const useRefferModal = ref(false);
export {
  useModal,
  useEditModal,
  useShareModal,
  useCreateModal,
  useCloseModal,
  useDeleteModal,
  useRefferModal,
};
